# Database Configuration
# For local development (if using local MongoDB):
# MONGODB_URI=mongodb://localhost:27017/portfolio

# For production, use your MongoDB Atlas connection string:
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/portfolio?retryWrites=true&w=majority

# Email Configuration (for contact form)
# Use your Gmail account credentials
EMAIL_USER=your-email@gmail.com
# Generate an App Password from Google Account settings (requires 2FA)
EMAIL_PASS=your-16-character-app-password

# Optional: Custom email service configuration
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587

# Next.js Configuration
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=your-secret-key

# Optional: Analytics and monitoring
# VERCEL_ANALYTICS_ID=your-analytics-id